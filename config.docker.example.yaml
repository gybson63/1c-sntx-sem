# Пример конфигурации для Docker (скопируйте в config.yaml).
# Пути внутри контейнера — см. volumes в docker-compose.yml.

platform_version: "8.3.27"
hbk_dir: /hbk
data_dir: /data
export_dir: /data/export
index_dir: /data/index

local_configs: []

bsp:
  enabled: false

embedding:
  provider: sentence_transformers
  model: intfloat/multilingual-e5-base
  device: cpu
  # Для openai_compatible при индексации ~30k чанков уменьшите batch_size и увеличьте timeout:
  # batch_size: 16
  # timeout: 600

api:
  host: 0.0.0.0
  port: 8000

# Для Docker с лимитом RAM ~4 ГБ: IVF-индекс LanceDB отключён (поиск без ANN, ~28k чанков ок).
# Docker Desktop: выделите 8 ГБ+ RAM для domain=all и IVF rebuild.
search:
  build_vector_index: false

mcp:
  log_level: INFO
  log_file: ""
